# .github/workflows/codeql.yml
name: CodeQL

on:
  workflow_run:
    workflows: ["Test"]
    types:
      - completed

jobs:
  analyze:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running CodeQL analysis"
